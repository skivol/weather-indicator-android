<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/appwidget_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/widget_margin"
    android:background="@drawable/widget_background"
    android:gravity="center"
    android:orientation="vertical">

    <TextView
        android:id="@+id/temperature_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:textColor="#FFF"
        android:textSize="16sp"
        tools:text="Temperature" />
    <!-- TODO Adjust the size of message. With "error message" android:textSize="24sp" is too much. Checkout "Autosizing TextViews" -->

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/date_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#FFF"
            android:textSize="12sp"
            tools:text="Date" />

        <ProgressBar
            android:id="@+id/load_progressbar"
            android:layout_width="18dp"
            android:layout_height="18dp" />

        <!-- TODO Note: vector image (using srcCompat) is not properly displayed... -->
        <ImageButton
            android:id="@+id/refresh_button"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:contentDescription="@string/refresh_button_text"
            android:src="@drawable/ic_refresh_white_24dp" />
    </LinearLayout>
</LinearLayout>
